{
    "name": "midnight-radio-contracts",
    "version": "1.0.0",
    "description": "Smart contracts for Midnight Radio on Mantle",
    "scripts": {
        "compile": "hardhat compile",
        "test": "hardhat test",
        "deploy:testnet": "hardhat run scripts/deploy.ts --network mantleTestnet",
        "deploy:mainnet": "hardhat run scripts/deploy.ts --network mantle"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "@openzeppelin/contracts": "^5.0.0",
        "hardhat": "^2.19.0",
        "dotenv": "^16.0.0"
    }
}